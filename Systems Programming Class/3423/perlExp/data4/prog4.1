#!/usr/bin/perl -w

while($file = <ARGV>){
	my(@lines) = $file;
}

#open(MYINPUTFILE, "<file1"); # open for input
#my(@lines) = <MYINPUTFILE>; # read file into list
@lines = sort(@lines); # sort the list
#my($line);
foreach (@lines) # loop thru list
 {
 print "@line\n"; # print in sort order
 }
#close(MYINPUTFILE);
